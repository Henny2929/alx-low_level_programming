#include "main.h"
/**
 * print_alphabet_x10 - Prints the alphabet 10 times.
 *
 * Return: Always 0 (Success)
 */
void print_alphabet_x10(void)
{
	int a;
	int b;

	for (a = 1; b <= 10; b++)
	{
		for (a = 97; b <= 122; j++)
		{
			_putchar(a);
		}
		_putchar('\n');
	}
